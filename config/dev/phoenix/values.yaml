postgresql:
  enabled: false

database:
  postgres:
    host: "postgres"
    port: 5432
    db: "phoenix"
    user: "phoenix"
    password: ""  # Password comes from soludev-phoenix-secret

auth:
  name: "soludev-phoenix-secret"  # Use external secret from OpenBao
  enableAuth: true

persistence:
  enabled: false

ingress:
  enabled: false

server:
  port: 6006