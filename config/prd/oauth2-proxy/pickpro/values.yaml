config:
  existingSecret: "oauth2-proxy-secret"
  existingConfig: "oauth2-proxy-config"
  cookieName: "oauth2-proxy-pickpro"

alphaConfig:
  enabled: true
  existingConfig: "oauth2-proxy-alpha-config"

sessionStorage:
  type: redis
  redis:
    clientType: standalone
    standalone:
      connectionUrl: redis://100.64.0.7:6379

ingress:
  enabled: false

resources:
  requests:
    cpu: "200m"
    memory: "64Mi"
  limits:
    cpu: "1000m"
    memory: "128Mi"

service:
  type: ClusterIP
  portNumber: 4180