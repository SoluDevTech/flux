apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pgbouncer-external-secret
  namespace: soludev
spec:
  refreshInterval: 60s
  secretStoreRef:
    name: openbao-backend
    kind: ClusterSecretStore
  target:
    name: pgbouncer-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: soludev/logto
