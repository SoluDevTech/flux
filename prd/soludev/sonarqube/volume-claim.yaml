apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarqube-sonarqube
  namespace: soludev
spec:
  volumeName: nfs-soludev-sonarqube
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-soludev-sonarqube
  resources:
    requests:
      storage: 5Gi