apiVersion: v1
kind: Service
metadata:
  name: headlamp
  namespace: soludev
  labels:
    app: headlamp
spec:
  type: ClusterIP
  selector:
    app: headlamp
  ports:
    - port: 80
      targetPort: 4466
      protocol: TCP
      name: http
