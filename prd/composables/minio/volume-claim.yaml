apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  namespace: composables
spec:
  volumeName: nfs-composables-minio
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-composables-minio
  resources:
    requests:
      storage: 5Gi
