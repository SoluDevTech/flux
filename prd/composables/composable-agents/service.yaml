apiVersion: v1
kind: Service
metadata:
  name: composable-agents
  namespace: composables
spec:
  selector:
    app: composable-agents
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
