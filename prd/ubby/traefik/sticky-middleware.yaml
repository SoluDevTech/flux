apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ubby-mcp-api-sticky
  namespace: ubby
spec:
  loadBalancer:
    sticky:
      cookie:
        name: mcp-route
        httpOnly: true
        secure: true
        sameSite: lax