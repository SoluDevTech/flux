apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: composables-postgres-pvc
  namespace: composables
spec:
  volumeName: nfs-composables-postgres
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-composables-postgres
  resources:
    requests:
      storage: 5Gi
