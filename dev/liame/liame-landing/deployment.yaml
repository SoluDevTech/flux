apiVersion: apps/v1
kind: Deployment
metadata:
  name: liame-landing
  namespace: liame
  labels:
    app: liame-landing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: liame-landing
  template:
    metadata:
      labels:
        app: liame-landing
    spec:
      tolerations:
        - key: "klipperlb-exclude"
          value: "true"
          effect: "NoSchedule"
          operator: "Equal"
      containers:
        - name: liame-landing
          image: kaiohz/liam-landing:latest
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "96Mi"
              cpu: "100m"
