apiVersion: v1
kind: Service
metadata:
  name: liam-landing
  namespace: liame
  labels:
    app: liam-landing
spec:
  type: ClusterIP
  selector:
    app: liam-landing
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
