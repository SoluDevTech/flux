apiVersion: v1
kind: Service
metadata:
  name: prospectio-landing
  namespace: prospectio
  labels:
    app: prospectio-landing
spec:
  type: ClusterIP
  selector:
    app: prospectio-landing
  ports:
    - port: 5173
      targetPort: 80
      protocol: TCP
      name: http
