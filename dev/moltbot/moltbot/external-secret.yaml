apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: moltbot-external-secret
  namespace: moltbot
spec:
  refreshInterval: 60s
  secretStoreRef:
    name: openbao-backend
    kind: ClusterSecretStore
  target:
    name: moltbot-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: moltbot/moltbot
