apiVersion: v1
kind: Service
metadata:
  name: moltbot
  namespace: moltbot
  labels:
    app: moltbot
spec:
  selector:
    app: moltbot
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
    - name: bridge
      port: 18790
      targetPort: 18790
  type: ClusterIP
