apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  namespace: pickpro
spec:
  volumeName: nfs-pickpro-minio
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-pickpro-minio
  resources:
    requests:
      storage: 5Gi
