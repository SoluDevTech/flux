apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  namespace: pickpro
spec:
  volumeName: nfs-dev-pickpro-minio
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-dev-pickpro-minio
  resources:
    requests:
      storage: 5Gi