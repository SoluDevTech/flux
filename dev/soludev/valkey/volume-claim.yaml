apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valkey
  namespace: soludev
spec:
  volumeName: nfs-dev-valkey
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-dev-valkey
  resources:
    requests:
      storage: 1Gi