apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openobserve-data
  namespace: soludev
spec:
  volumeName: nfs-dev-openobserve
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-dev-openobserve
  resources:
    requests:
      storage: 10Gi