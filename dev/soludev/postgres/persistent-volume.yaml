apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-soludev-postgres
  namespace: soludev
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-soludev-postgres
  nfs:
    server: 100.64.0.4
    path: /home/lima.linux/k3s-storage/soludev/postgres
  mountOptions:
    - nfsvers=4.1
    - hard
    - timeo=600
    - retrans=2
