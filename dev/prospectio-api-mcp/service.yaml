apiVersion: v1
kind: Service
metadata:
  name: prospectio-api-mcp
  labels:
    app: prospectio-api-mcp
spec:
  type: ClusterIP
  selector:
    app: prospectio-api-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
