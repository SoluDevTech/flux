apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubby-mcp-api
  namespace: ubby
  labels:
    app: ubby-mcp-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ubby-mcp-api
  template:
    metadata:
      labels:
        app: ubby-mcp-api
        app.kubernetes.io/part-of: soludev-workload
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/part-of: soludev-workload
      containers:
        - name: ubby-mcp-api
          image: kaiohz/pickpro:ubby-mcp-api-c5e87b3b3c33e5b54a8844af2350d9daa874a3c6
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: ubby-mcp-api-config
            - secretRef:
                name: ubby-mcp-api-secret
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 90
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 60
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
      imagePullSecrets:
        - name: ubby-docker-secret
