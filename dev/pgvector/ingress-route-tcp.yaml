apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: pgvector-tcp
spec:
  entryPoints:
    - postgres
  routes:
    - match: HostSNI(`*`)
      services:
        - name: pgvector
          port: 5432