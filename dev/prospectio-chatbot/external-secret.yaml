apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prospectio-chatbot-secret
  namespace: kaiohz
spec:
  refreshInterval: 60s
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: prospectio-chatbot-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: prospectio/chatbot