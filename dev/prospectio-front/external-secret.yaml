apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prospectio-front-external-secret
  namespace: kaiohz
spec:
  refreshInterval: 60s
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: prospectio-front-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: prospectio/front