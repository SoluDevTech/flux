apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prospectio-front-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/redirect-scheme: https
spec:
  rules:
    - host: prospectio.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prospectio-front
                port:
                  number: 5173