apiVersion: v1
kind: ConfigMap
metadata:
  name: prospectio-front-config
data:
  config.json: |
    {
      "chatbotUrl": "http://prospectio.chatbot.local",
      "backendUrl": "http://prospectio.api-mcp.local"
    }
